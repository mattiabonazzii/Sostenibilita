extends layout

block content
  h1=layout
  h1= post.title
  h3 Autore: #{post.author} | Data: #{post.date} | Categoria: #{post.category}
  //- Visualizzazione del contenuto del post
  div(class="text-center")
    div(class="contornopost")
      p= post.content

  include style.pug
